{
  "concept": "arrays",
  "category": "data_structures",
  "description": "Arrays are contiguous memory structures that store elements of the same type and provide constant-time indexing.",
  "languages": {
    "python": {
      "examples": {
        "basic": "arr = [1, 2, 3]\nprint(arr[0])  # indexing",
        "advanced": "# region slicing\narr = [1, 2, 3, 4]\nsub = arr[1:3]\n# endregion",
        "realworld": "data = load_values()\nfirst = data[0]"
      }
    },
    "javascript": {
      "examples": {
        "basic": "const arr = [1, 2, 3];\nconsole.log(arr[0]);",
        "advanced": "// region map/filter\nconst doubled = arr.map(x => x * 2);\nconst evens = arr.filter(x => x % 2 === 0);\n// endregion",
        "realworld": "const users = fetchUsers();\nconst first = users[0];"
      }
    },
    "go": {
      "examples": {
        "basic": "arr := [3]int{1, 2, 3}\nfmt.Println(arr[0])",
        "advanced": "// region slices\ns := []int{1,2,3}\ns = append(s, 4)\n// endregion",
        "realworld": "values := loadValues()\nfirst := values[0]"
      }
    },
    "cpp": {
      "examples": {
        "basic": "int arr[3] = {1,2,3};\nstd::cout << arr[0];",
        "advanced": "// region vector usage\nstd::vector<int> v = {1,2,3};\nv.push_back(4);\n// endregion",
        "realworld": "std::vector<int> data = loadValues();\nauto first = data[0];"
      }
    }
  }
}
